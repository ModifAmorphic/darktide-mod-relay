# Release pipeline — release-please (simple, path-based) + chained Windows build.
#
# release-please versions on changes under src/ (Rust + C + Lua all count),
# cuts the tag + GitHub release, then the build-assets job (same workflow,
# gated on releases_created) builds the Windows runtime bundle and attaches it.
#
# In-workflow chaining (not a separate on: release workflow) is deliberate:
# resources created with the default GITHUB_TOKEN don't trigger other
# workflows, so a separate on:release job would never fire without a PAT.
name: release-please

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      releases_created: ${{ steps.release.outputs.releases_created }}
      # Package path is `src`, so the per-path output is `src--tag_name`.
      tag_name: ${{ steps.release.outputs['src--tag_name'] }}
    steps:
      - uses: googleapis/release-please-action@v5
        id: release
        with:
          config-file: .release-please-config.json
          manifest-file: .release-please-manifest.json
          # The short ref name of the branch or tag that triggered
          #  the workflow run. For example, `main` or `1.x`
          target-branch: ${{ github.ref_name }}

  # Build the Windows runtime bundle and attach it to the release release-please
  # just created. Windows-only: the shipped build is the MSVC/native one.
  # Runtime bundle includes executables, mod loader Lua, and legal notices
  # (LICENSE and THIRD_PARTY_NOTICES.md).
  build-assets:
    needs: release-please
    if: ${{ needs.release-please.outputs.releases_created == 'true' }}
    runs-on: windows-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: src
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src/target
          key: release-msvc-${{ hashFiles('src/Cargo.lock') }}
      - name: Build Rust staticlib + C DLL + launcher + stage assets (MSVC)
        shell: powershell
        run: ./build.ps1 build
      - name: Package release zip (flat layout)
        shell: bash
        run: |
          cd bin
          7z a "../${{ needs.release-please.outputs.tag_name }}-windows-x64.zip" \
            relay_shell.dll mod_relay.exe mod_loader/ LICENSE THIRD_PARTY_NOTICES.md
      - name: Upload to release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ needs.release-please.outputs.tag_name }}" \
            "${{ needs.release-please.outputs.tag_name }}-windows-x64.zip" --clobber
